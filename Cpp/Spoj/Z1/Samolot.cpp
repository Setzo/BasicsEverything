#include <stdio.h> 
#include <stdlib.h> 
int main(int argc, char *argv[]) { 
        int n1, n2, k1, k2, x=0; 
        scanf("%d %d %d %d", &n1, &k1, &n2, &k2); 
        wynik=(n1*k1)+(n2*k2); 
        printf("%d", x); 
        return 0; 
}
